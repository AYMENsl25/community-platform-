[project]
name = "talaqi-worker"
version = "0.0.0"
requires-python = ">=3.13,<3.14"
dependencies = ["talaqi-api"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/talaqi_worker"]
